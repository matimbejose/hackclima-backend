; Enable GD extension for JPEG support
; Note: The following extension name might differ depending on the PHP version and configuration
; For PHP 8.1 FPM, the extension name is 'gd'
; For PHP 7.x, the extension name is 'gd2'
; Change it accordingly if needed.
extension=gd
